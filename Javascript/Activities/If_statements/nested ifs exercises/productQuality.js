//Javascript prgram that checks the quality of the products 

//Using nested ifs 

let expired = true;
let quality = 7;

if (expired = true) {
    if (quality > 8) {
        console.log("Good Quality");  

    } else if (quality >5 && quality < 8) {
        console.log("Average Quality");
        
    } else {
        console.log("Poor Quality");
        
    }
} else {
    console.log("Donot use");
    
}